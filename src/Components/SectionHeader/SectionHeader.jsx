

const SectionHeader = ({heading}) => {
    return (
        <div className=" my-10  text-center">
            <h3 className="uppercase text-3xl py-3 mb-3">{heading}</h3>
            
        </div>
    );
};

export default SectionHeader;